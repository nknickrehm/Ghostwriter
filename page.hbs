{{!< default}}

{{#post}}
    <article class="{{page_class}}">
        <header class="wrapper">
            <article class="w-full">
                <h1 class="text-5xl font-bold mb-5 leading-tight">{{title}}</h1>

                {{#if tags}}
                    <div class="mb-5">
                        <a class="button bg-gray-100" disabled="true"><i class="fas fa-tags"></i></a>
                        {{#foreach tags}}
                            {{> tag}}
                        {{/foreach}}
                    </div>
                {{/if}}

                {{#if custom_excerpt}}
                    <p class="mb-5">{{custom_excerpt}}</p>
                {{/if}}
            </article>

            {{#if feature_image}}
                <img class="w-full pt-5"
                     srcset="{{img_url feature_image size="s"}} 300w,
                            {{img_url feature_image size="m"}} 600w,
                            {{img_url feature_image size="l"}} 1000w,
                            {{img_url feature_image size="xl"}} 2000w"
                     sizes="(max-width: 1000px) 400px, 800px"
                     src="{{img_url feature_image size="m"}}"
                     alt="{{title}}"
                     loading="lazy"
                />
            {{/if}}
        </header>

        <div class="grid w-screen left-0 my-10 content">
            {{content}}
        </div>
    </article>
{{/post}}
